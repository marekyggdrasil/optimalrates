First generate the rates in universal format that can be used to generate CSP model.

`node generate.js`

Then you can specify your instance in `instance.json`. To see optimal solutions simply launch:

`node solver.js`
